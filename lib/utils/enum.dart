enum ShopCardType { productGrid, videoOverlay, productRow }

enum RecentlyViewType { product, shop }

enum OrderStatuses {
  pending,
  confirmed,
  processing,
  shipped,
  delivered,
  cancelled,
  refunded,
}

enum PaymentStatuses {
  pending,
  processing,
  success,
  failed,
  cancelled,
  abandoned,
}
